# Secrets — never commit
.env
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Personal prep notes (Loom scripts) — not a deliverable
LOOM_SCRIPTS.md

# Keep call artifacts (recordings + transcripts) in the repo — they are deliverables.
# Only ignore the raw temp downloads.
calls/**/*.wav
calls/**/_raw_*
